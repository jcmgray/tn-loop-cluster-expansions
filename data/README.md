These are `h5netcdf` files. See xarray and xyzpy to load them for example.

- f"energy_cluster_{model}_{geom}.h5" contains single cluster data
- f"energy_full_{model}_{geom}.h5" contains reference boundary contraction data
- f"energy_gloop_expand_{model}_{geom}.h5" contains the loop cluster expansion data